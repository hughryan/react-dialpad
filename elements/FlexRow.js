import styled from 'styled-components';

const FlexRow = styled.div`
	display: flex;
	position: relative;
	flex-direction: row;
	overflow: hidden;
	font-family: "Arial";
	font-size: 14px;
	padding-bottom: 10px;
`;

export default FlexRow;
